#include <stdio.h>
int main()
{
   int n,x,i;
   scanf("%d%d",&n,&x);
   int arr[n];
   for(i=0;i<n;i++)
   {
    scanf("%d",&arr[i]);  
   }
   for(i=0;i<n;i++)
   {
    if(arr[i]==x)
    {
       printf("element found at index:%d",i);
    }
   }
    return 0;
}
